# Review Themes

Last updated: Project scaffolding and local app bootstrap

1. **Interaction state must match UI lifecycle** + Gate visual effects and stateful output behind an explicit trigger so they activate only after the user interaction that creates them.
